/**
 * Single diameter + flex chrome for the home/room header icon row (theme,
 * share, settings, logout, initials avatar) and the room facepile entry
 * chips. Keeps every circle the same outer radius across breakpoints.
 */
export const HEADER_CLUSTER_CIRCLE_LAYOUT =
  "inline-flex aspect-square h-9 w-9 min-h-9 min-w-9 shrink-0 items-center justify-center rounded-full sm:h-10 sm:w-10 sm:min-h-10 sm:min-w-10";
